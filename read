import { RepeatTypeComponent } from './components/repeat/repeat-section.type';


UsbActivityLogComponent,
    RepeatTypeComponent
    
    
    
    
    FormlyModule.forRoot({  types: [{name: 'repeat', component: RepeatTypeComponent}], extras: { lazyRender: true } }),


let option= [];
          field.templateOptions.options.map(ele=>option.push(ele));
          let index = option.findIndex((e)=>e.value==model.fieldDeletePath);
          return index > -1 ? field.templateOptions.options[index].example : "";
